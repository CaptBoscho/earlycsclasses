package client;

import javax.swing.JFrame;

public class LoginParentFrame extends JFrame{

	public LoginParentFrame(String hostname, int port)
	{
		new LoginFrame(this, hostname, port);
		setDefaultCloseOperation(EXIT_ON_CLOSE);
	}
}
